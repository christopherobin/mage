archivist:
    vaults:
        mysqlVault:
            type: mysql
            config:
                options:
                    host: "localhost"
                    user: "root"
                    password: null
                    database: "mage_unit_tests"