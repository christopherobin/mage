archivist:
    vaults:
        mysqlVault:
            type: mysql
            config:
                url: "mysql://root:aeriscloud@localhost/mage_unit_tests"

logging:
    server:
        terminal:
            channels: ">=alert"
    html5:
        server:
            channels: ">=warning"

server:
    clientHost:
        bind:
            file: null
            port: 0
        expose: null
    cluster: false
