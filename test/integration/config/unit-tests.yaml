logging:
    server:
        terminal:
            config: {}
    html5:
        server:
            channels: ">=warning"

server:
    clientHost:
        bind:
            file: null
            port: 0
        expose: null
    cluster: false
