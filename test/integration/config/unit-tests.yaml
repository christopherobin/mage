logging:
    server:
        terminal:
            channels:
                - ">=debug"

server:
    clientHost:
        bind:
            file: null
            port: 0
        expose: null
    cluster: false
