archivist:
    vaults:
        mysqlVault:
            type: mysql
            config:
                options:
                    host: "localhost"
                    port: "3306"
                    user: "root"
                    password: "aeriscloud"
                    database: "mage_unit_tests"
    listOrder:
        - volatileVault
        - mysqlVault
    readOrder:
        - volatileVault
        - mysqlVault
    writeOrder:
        - client
        - volatileVault
        - mysqlVault

server:
    clientHost:
        bind:
            port: 8080
