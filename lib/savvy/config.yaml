# Where to put the savvy server.
bind:
    file: ./savvy.sock

# Expose may be a full URL, or an object like:
#   host: localhost
#   port: 80
#   path: /path/to/stuff
#   authUser: harry
#   authPass: 123456abc
expose: "http://localhost"
