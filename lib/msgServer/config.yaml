clientHost:
    protocol: http
    transports:
        longpolling:
            heartbeat: 120
    bind:
        protocol: http
        file: ./server.sock

# You will need to add a host and port to expose on.
# expose:
# 	host: sandbox.me.node.wizcorp.jp
# 	port: 80
