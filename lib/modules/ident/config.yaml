topic: ident
engines:
    mageUsernames:
        type: userpass
        saltSize: 32
        config:
            hmac:
                algorithm: sha256
                key: somelongkeythatnoonewillguess

apps:
    dev:
        default:
            type: anonymous
            config:
                access: admin

dashboard:
    pages:
        ident:
            name: Ident
            listed: true
            apps:
                - dev
