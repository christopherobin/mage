topic: ident

apps:
    dev:
        default:
            type: anonymous
            config:
                access: admin

dashboard:
    pages:
        ident:
            name: Ident
            listed: true
            apps:
                - dev
